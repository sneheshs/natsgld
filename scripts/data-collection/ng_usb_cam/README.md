# Installing ROS USB Camera package
To use usb_cam ros node, simply install ros-kinetic-usb-cam:<br/>
```sudo apt-get install ros-kinetic-usb-cam```

# Running the usb-cam node
```roslaunch ng_usb_cam.launch```

